import axiosInstance from "./axiosInstance";

export const GetAddlistingUpload = async ()=> {
    
    try {
    const response = axiosInstance.post('upload-listing-files', {
        
    })   
    } catch (error) {
        
    }
}